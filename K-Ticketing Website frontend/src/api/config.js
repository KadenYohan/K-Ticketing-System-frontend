// K-Ticketing Website — API Configuration
// Toggle USE_MOCK to false when connecting to the live Lubuntu backend.
export const CONFIG = {
  USE_MOCK: true,
  API_BASE_URL: 'http://localhost:3000',
};
